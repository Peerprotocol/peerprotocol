import React from "react"

export default function Loading() {
    return (
        <div className="w-full h-[100vh] flex flex-col items-center justify-center text-white bg-black text-[1.2rem] font-semibold">
            <p className="self-center">Loading...</p>
        </div>
    )
  }